---
description: Комит, Пуш и Деплой (КПД) - автоматическая публикация изменений
---

# КПД: Комит + Пуш + Деплой

Автоматически выполняет полный цикл публикации изменений:
1. Создает commit с правильным Conventional Commits форматом
2. Push в main
3. Deploy автоматически через GitHub Actions

## Инструкции

### 1. Проверка изменений

```bash
git status
git diff --stat
```

**Если нет изменений:**
```
⚠️ Нет незакоммиченных изменений.
```

### 2. Анализ изменений

- Прочитай git diff чтобы понять что изменилось
- Определи тип commit: `feat`, `fix`, `docs`, `style`, `refactor`, `chore`
- Определи scope (например: `hero`, `nav`, `ui`, `build`)
- Составь краткое описание

### 3. Создание commit

**Формат:**
```
<type>(<scope>): <subject>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

**Выполни commit:**
```bash
git add .
git commit -m "$(cat <<'EOF'
<type>(<scope>): <subject>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

### 4. Push изменений

```bash
git push origin main
```

### 5. Итоговый вывод

```markdown
✅ КПД завершен успешно!

**Commit:** <commit-hash>
**Message:** <type>(<scope>): <subject>

**Push:** ✅ Отправлен в main

**Deploy:**
- 🔄 GitHub Actions запущен автоматически
- ⏱️ Деплой займёт ~1-2 минуты
- 🌐 Сайт: https://metacell.ru

Проверь статус деплоя: https://github.com/<user>/Metacell-landing/actions
```

## Важно

- ⚠️ ВСЕГДА проверяй git status перед commit
- ⚠️ ВСЕГДА используй Conventional Commits формат
- ⚠️ НЕ делай `git push --force` без подтверждения
- ⚠️ НЕ коммить `.env` или secrets
- ✅ Деплой автоматический при push в main
